# Week 2 Deliverable: APIs & LLM Calls

This repository contains my submission for Week 2, focused on setting up a Python environment and making Gemini API calls.

## Project Description
A working Python script that takes a user-defined topic from the terminal and utilizes the official Google GenAI SDK (`gemini-2.5-flash`) to generate a concise summary strictly limited to exactly 3 sentences.

## How to Run the Script

1. **Install Dependencies:**
   ```bash
   pip install google-genai
  ```markdown
2. **Configure API Key:**
   Open `agent_summary.py` and replace `"YOUR_API_KEY_HERE"` with your actual Google AI Studio API key.

3. **Execute:**
   ```bash
   python agent_summary.py
